(function($) {
    // page controller
    $.fn.controller = function() {
        var api = {};
        // initialize controls with server default values
        var init = function() {
        };
        // init the controller
        init();
        // for public functions and vars
        return api;
    };
})($);
